{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build active file",
            "command": "/usr/bin/gcc",
            "args": [
                "-g",
                "${workspaceFolder}/states.c",
                "${workspaceFolder}/ErrorHandling.c",
                "${workspaceFolder}/scanner.c",
                "-o",
                "${workspaceFolder}/scanner"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ]
}